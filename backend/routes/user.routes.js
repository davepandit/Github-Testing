import express from 'express'

//making a router
const router = express.Router()

// making some routes 
// router.route('/getBlogs').post(add some middlewares and then the main controller).get(here also and then make some sort of the controller )

// router needs to be exported as default 